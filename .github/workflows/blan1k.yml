on:
   pull_request:
    types: [closed]
    branches:
    - 'main'
   workflow_dispatch:
jobs:  
  android-alpha-distribution:
    if: ${{ github.event.pull_request.title =~ \[ANDROID-[0-9]+\.[0-9]+\.[0-9]+\] }}
  
    runs-on: ubuntu-latest
    environment: alpha
    steps:
      - name: Set version
        id: set_version
        run: |
            echo "Hola mundo"
