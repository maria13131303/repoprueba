on:
  pull_request:
    types: [closed]
    branches:
      - 'main'
  workflow_dispatch:

jobs:
  android-alpha-distribution:
    if:  ${{ contains(github.event.pull_request.title, 'android') && github.event.pull_request.merged == true }}
    runs-on: ubuntu-latest
    environment: alpha
    steps:
      - name: Set version
        id: set_version
        run: |
          pr_title="${{ github.event.pull_request.title }}"
          android_version=$(echo "$pr_title" | sed -nE 's/.*v\s*([0-9]+\s*\.\s*[0-9]+\s*\.\s*[0-9]+).*/\1/p' | cut -d' ' -f2 | tr -d '[:space:]')
          if [[ -n "$android_version" ]]; then
            echo "version=$android_version" >> $GITHUB_ENV
          else
            echo "The PR name does not contain a valid Android version"
            exit 1
          fi
      
      - name: Print output
        run: |
          version=$(echo "${{ env.version }}" | cut -d'=' -f2)
          echo "La versión de Android es: $version"
